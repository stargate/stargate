query GetOrdersWithMax($filter: OrdersFilterInput) {
    Orders(filter: $filter) {
        pageState
        values {
            id
            prodId
            prodName
            customerName
            address
            description
            price
            sellPrice
            max: _decimal_function(name: "max", args: ["price"])
        }
    }
}