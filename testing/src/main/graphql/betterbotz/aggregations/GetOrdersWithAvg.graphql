query GetOrdersWithAvg($filter: OrdersFilterInput) {
    Orders(filter: $filter) {
        pageState
        values {
            id
            prodId
            prodName
            customerName
            address
            description
            price
            sellPrice
            _decimal_function(name: "avg", args: ["price"])
        }
    }
}