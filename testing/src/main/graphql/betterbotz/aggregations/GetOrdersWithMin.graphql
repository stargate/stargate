query GetOrdersWithMin($filter: OrdersFilterInput) {
    Orders(filter: $filter) {
        pageState
        values {
            id
            prodId
            prodName
            customerName
            address
            description
            price
            sellPrice
            min: _decimal_function(name: "min", args: ["price"])
        }
    }
}