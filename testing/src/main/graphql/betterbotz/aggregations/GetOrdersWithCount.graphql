query GetOrdersWithCount($value: OrdersInput, $aggregationField: String!) {
    Orders(value: $value) {
        pageState
        values {
            id
            prodId
            prodName
            customerName
            address
            description
            price
            sellPrice
            count: _int_function(name: "count", args: [$aggregationField])
        }
    }
}