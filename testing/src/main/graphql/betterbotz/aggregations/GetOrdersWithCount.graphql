query GetOrdersWithCount($filter: OrdersFilterInput) {
    Orders(filter: $filter) {
        pageState
        values {
            id
            prodId
            prodName
            customerName
            address
            description
            price
            sellPrice
            count: _bigint_function(name: "count", args: ["description"])
        }
    }
}