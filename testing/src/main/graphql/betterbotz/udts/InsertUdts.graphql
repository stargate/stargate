mutation InsertUdts(
    $value: UdtsInput!
    $ifNotExists: Boolean
    $options: MutationOptions
) {
    insertUdts(
        value: $value
        ifNotExists: $ifNotExists
        options: $options
    ) {
        applied
    }
}