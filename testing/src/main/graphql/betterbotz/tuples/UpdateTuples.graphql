mutation UpdateTuples(
    $value: Tuplx65_sInput!
    $ifExists: Boolean
    $options: MutationOptions
) {
    updateTuplx65_s(
        value: $value
        ifExists: $ifExists
        options: $options
    ) {
        applied
    }
}