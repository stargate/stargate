mutation UpdateTuples(
    $value: TuplesInput!
    $ifExists: Boolean
    $options: MutationOptions
) {
    updateTuples(
        value: $value
        ifExists: $ifExists
        options: $options
    ) {
        applied
    }
}