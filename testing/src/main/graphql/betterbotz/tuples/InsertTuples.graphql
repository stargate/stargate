mutation InsertTuples(
    $value: Tuplx65_sInput!
    $ifNotExists: Boolean
    $options: MutationOptions
) {
    insertTuplx65_s(
        value: $value
        ifNotExists: $ifNotExists
        options: $options
    ) {
        applied
    }
}