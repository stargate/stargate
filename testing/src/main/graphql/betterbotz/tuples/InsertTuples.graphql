mutation InsertTuples(
    $value: TuplesInput!
    $ifNotExists: Boolean
    $options: MutationOptions
) {
    insertTuples(
        value: $value
        ifNotExists: $ifNotExists
        options: $options
    ) {
        applied
    }
}