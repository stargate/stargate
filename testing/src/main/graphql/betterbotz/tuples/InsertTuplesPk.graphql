mutation InsertTuplesPk(
    $value: TuplesPkInput!
    $ifNotExists: Boolean
    $options: MutationOptions
) {
    insertTuplesPk(
        value: $value
        ifNotExists: $ifNotExists
        options: $options
    ) {
        applied
    }
}