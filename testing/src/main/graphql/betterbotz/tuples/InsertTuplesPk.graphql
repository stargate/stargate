mutation InsertTuplesPk(
    $value: Tuplx65_sPkInput!
    $ifNotExists: Boolean
    $options: MutationOptions
) {
    insertTuplx65_sPk(
        value: $value
        ifNotExists: $ifNotExists
        options: $options
    ) {
        applied
    }
}