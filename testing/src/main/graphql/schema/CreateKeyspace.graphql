mutation CreateKeyspace(
    $name: String!
    $ifNotExists: Boolean
    $replicas: Int
    $datacenters: [DataCenterInput]
) {
    createKeyspace (
        name: $name,
        ifNotExists: $ifNotExists,
        replicas: $replicas,
        datacenters: $datacenters
    )
}
