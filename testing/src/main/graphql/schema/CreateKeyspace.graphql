mutation CreateKeyspace(
    $name: String!
    $ifNotExists: Boolean
    $replication: [ReplicationOptionInput]!
) {
    createKeyspace (
        name: $name,
        ifNotExists: $ifNotExists,
        replication: $replication
    )
}
