mutation AlterTableAdd(
    $keyspaceName: String!
    $tableName: String!
    $toAdd: [ColumnInput]!
) {
    alterTableAdd(
        keyspaceName: $keyspaceName
        tableName: $tableName
        toAdd: $toAdd
    )
}
